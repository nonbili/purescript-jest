{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "postinstall": "bower install",
    "test": "pulp build -I test && jest",
    "test:ci": "pulp build -I test -- --strict --stash +RTS -N2 -RTS"
  },
  "devDependencies": {
    "jest": "^25.1.0",
    "purescript-psa": "^0.7.3"
  }
}
